<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    <header>
        <h1>{{ title }}</h1>
        {% if message %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endif %}
    </header>

    <main>
        <h2>Produktliste</h2>
        <ul>
            {% for product in produkte %}
                <li>
                    <strong>{{ product.artikelbezeichnung }}</strong> ({{ product.artikelnummer }}) - {{ product.preis }} € - Hersteller: {{ product.hersteller }}
                </li>
            {% else %}
                <li>Keine Produkte gefunden.</li>
            {% endfor %}
        </ul>
    </main>
</body>
</html>


