<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Produktkatalog</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

    <header class="page-header">
        <h1>Produktkatalog</h1>
    </header>

    {% include 'navbar.html.twig' %}

    <main>
        <table>
            <thead>
                <tr>
                    <th>Artikelnummer</th>
                    <th>Artikelbezeichnung</th>
                    <th>Preis</th>
                    <th>Hersteller</th>
                </tr>
            </thead>
            <tbody>
                {% for product in products %}
                    <tr>
                        <td>{{ product.artikelnummer }}</td>
                        <td>{{ product.artikelbezeichnung }}</td>
                        <td class="price">{{ product.preis is not null ? (product.preis|number_format(2, ',', '.')) : '' }}</td>
                        <td class="hersteller">{{ product.hersteller }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="alert">Keine Produkte vorhanden.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </main>

</body>
</html>







