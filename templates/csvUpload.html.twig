<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Datei hochladen</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
</head>
<body>

    <header class="page-header">
        <h1>Datei hochladen</h1>
    </header>

    {% include 'navbar.html.twig' %}

    <main class="upload-form-container">
        <h2>{{ pimcore_input("myHeadline") }}</h2>

        <form action="{{ path('csv') }}" method="POST" enctype="multipart/form-data" class="upload-form">
            <div class="form-group">
                <label for="pdf" class="form-label">Wähle CSV Datei hoch:</label>
                <input type="file" id="pdf" name="pdf" class="form-input" required>
            </div>

            <button type="submit" class="submit-button">Hochladen</button>
        </form>

        {% if success %}
            <div class="success-message">
                <p>{{ success }}</p>
            </div>
        {% endif %}
    </main>

    <script src="/js/main.js"></script>
</body>
</html>



